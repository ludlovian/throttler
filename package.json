{
  "name": "throttler",
  "version": "1.2.0",
  "description": "Rate limiting transform stream",
  "main": "src/index.js",
  "module": "src/index.js",
  "source": "src/index.js",
  "type": "module",
  "scripts": {
    "clean": "rm -rf node_modules/.cache/ coverage/",
    "format": "prettier-standard {src,test}/*.mjs",
    "lint": "standard {src,test}/*.mjs | snazzy",
    "test": "c8 ava",
    "coverage": "npm run test && c8 report --reporter=html && sirv coverage -H '0.0.0.0' -p 8000"
  },
  "author": "",
  "license": "Unlicense",
  "devDependencies": {
    "ava": "^3.10.1"
  }
}
